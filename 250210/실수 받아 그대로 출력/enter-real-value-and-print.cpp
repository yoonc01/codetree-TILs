#include <iostream>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    double num;
    
    cout << fixed;
    cout.precision(2);

    cin >> num;
    cout << num << endl;
    return 0;
}